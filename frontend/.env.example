# Public build-time API origin. Required for production builds; use HTTPS in production.
VITE_API_URL=http://localhost:4000
# Set true in production only when the backend Google OAuth routes are configured.
VITE_GOOGLE_AUTH_ENABLED=true
